# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores - file extension patterns
*.import
*.uid
export.cfg
export_credentials_cfg
*.tmp

# Server binaries — Docker downloads Godot headless at build time
Server/*.x86_64


# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Ziva
addons/ziva_agent/

# Editor/OS
.DS_Store
Thumbs.db
nul

# Ziva runtime / build junk
server_output.log
server_test.log
server_waker.log

# Server binaries (compiled) - she does not need these to edit the game
server_launcher/

# Prebuilt compiled server binaries (edit from source, don't track)
Server/*.pck
Server/*.x86_64
test_instances/

# Build artifacts
*.apk
*.apk.idsig
*.idsig
grasslands_build/
